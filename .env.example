# Supabase Configuration
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Cloudflare Worker Configuration (for Worker)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Cloudflare R2 Bucket (configured in wrangler.toml)
# CHATKIN_BUCKET is bound via wrangler.toml

# Development URLs
# The SvelteKit app will run on http://localhost:5173
# The Worker will run on http://localhost:8787
